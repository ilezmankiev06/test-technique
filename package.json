{
  "name": "cli-filter-count",
  "type": "module",
  "scripts": {
    "filter": "node app.js --filter=ry",
    "count": "node app.js --count",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
